// define the name of the challange
// define objects and arrays, data set in square brackets 
// add in for loop const row index in lots of treatsure 
// then do a for loop in the loop but for the column index
// conole log (lots treasure [rowindex][columnindex])
// eremeber your curly bracksts enclosing the row within a row. 
// in teh second loop must makesure only going round the initial loop
// then conosle log the inner loop out, console.log(colcell)
// if we want to reference the values and not updaye we can use the of syntax - for loop with of, whereas if we are changing the
// index then we need to use in. 
